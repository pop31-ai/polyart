# Журнал проверок логической сходимости

Проверки модулей polyart: документация ↔ код ↔ UML, грамматика ↔ парсер,
конвертер ↔ формат, мета-язык ↔ примеры.

Формат записи: `[ID] статус — что проверяли → результат (подробности)`.

---

## 2026-08-02 — обходной аудит

| ID | Статус | Проверка | Результат |
|----|--------|----------|-----------|
| L01 | ПРОВЕРЕНО | Синтаксис всех 48 модулей `.py` (ast.parse) | OK — все компилируются |
| L02 | ИСПРАВЛЕНО | Статья `articles/14_meta_language_design.txt` ↔ код `polyart_lang.py` | Статья описывала несуществующие возможности (`^`, `on`, `for`, `@register`, REPL, bytecode). Добавлен раздел 11 «Implementation Status»: перечислен реализованный поднабор и отложенные цели. Утверждение «module implements such a language» снято |
| L03 | ИСПРАВЛЕНО | UML `D92–D94` (Tokenizer.nextToken/peek, Context, Visitor) ↔ код | UML — проектные эскизы. Сходимость восстановлена через раздел 11 статьи 14: указано, что UML отражает целевую архитектуру, код — однопроходный список токенов + словарь переменных |
| L04 | ИСПРАВЛЕНО | Расширение `.polyart` | Коллизия устранена: мета-язык переведён на `.plang` (CLI-подсказка + пример), `.polyart` остаётся JSON-данными |
| L05 | ИСПРАВЛЕНО | Литеральные списки `{...}` на месте выражения | `parse_primary` не обрабатывал `LBRACE` → SyntaxError. Добавлена ветка ListNode; `polygon({...})`/`bezier({{...}})` работают |
| L06 | ИСПРАВЛЕНО | Бинарные литералы `true`/`false` | Парсились как VarNode → 0. Теперь `true`=1, `false`=0 в `parse_primary`; `circle(..., true, ...)` даёт заливку |
| L07 | ИСПРАВЛЕНО | Оператор диапазона `..` | 2 бага: (а) regex NUMBER `\d+\.?\d*` съедал первую точку `..` → токены DOT; исправлен на `\d+(\.\d+)?`. (б) вычисление диапазона ломалось для отрицательных: знаменатель `int(r-l)*10` → max(1, отриц)=1; переписано через `abs` и `n-1` |
| L08 | ИСПРАВЛЕНО | `pi`/`phi` как переменные | `vars` инициализируется `pi` и `phi`; работают и как переменные, и как `pi()` |
| L09 | ИСПРАВЛЕНО | Индексация списка `x[0]` | Добавлены токены `[`/`]`, узел `IndexNode`, парсинг в `parse_primary` и eval (список/кортеж/строка, с проверкой границ) |
| L10 | ПРОВЕРЕНО | `bezier` (CURVE3) | Не падает (проверено matplotlib 2 и 3 точками). Найден избыточный код `Path.CURVE3 if i % 2 == 1 else Path.CURVE3` — обе ветки одинаковы; геометрия гарантируется для пар вершин |
| L11 | ИСПРАВЛЕНО | Конвертер: ветка `.polyart`→`.polyart` | Удалена: писала PNG-байты в файл `.polyart` через `plt.savefig` (мёртвая/ошибочная ветка) |
| L12 | ИСПРАВЛЕНО | Конвертер: GIF/видео | GIF извлекал 1 кадр (FuncAnimation перечитывал файл); `.mp4` давал чёрный кадр. Переписан `load_video_frames`: PIL-итерация кадров GIF, imageio для видео, `max_frames`. Проверено: 56 кадров GIF → polyvid → GIF (раньше 1). Также исправлен `save_gif` (float-кадры `f*255`, а не `f/255*255`) |

## 2026-08-02 — второй проход (после исправлений)

| ID | Статус | Проверка | Результат |
|----|--------|----------|-----------|
| V01 | ПРОВЕРЕНО | Мета-язык: полный скрипт-пример | `examples/12_polyart_lang.plang` отработал (98 КБ PNG): canvas, golden_spiral, circle(fill), sine, polygon(list), range+index, bezier, repeat/$i, emotion, animal, pi, text, render, print |
| V02 | ПРОВЕРЕНО | Конвертер self-test (8 шагов) | OK: image→polyart(14 объектов)→png; frames→polyvid(24 кадра)→gif; ratio 0.23x (img), 6.96x (vid) |
| V03 | ПРОВЕРЕНО | GIF→polyvid→GIF round-trip | 03_column_build.gif: 56 кадров извлечено, конвертация и восстановление прошли |
| V04 | ПРОВЕРЕНО | requirements.txt ↔ использование | Добавлены pillow, imageio (используются конвертером и кодеком) |
| V05 | ОТКРЫТО | Индексация отрицательных индексов, `else` для `if` | Не реализованы; не противоречат документации (не заявлены). Занесено в раздел 11 статьи 14 как «not yet implemented» |
| V06 | ИСПРАВЛЕНО | Мост «мета-язык → .polyart JSON» | Добавлена команда `save_polyart(path)` в `polyart_lang.py`: экспорт текущего холста в канонический `.polyart` v2.0.0 (polyart_api/`polyart_format`). Художники matplotlib (линии, патчи, коллекции, заливки) аппроксимируются параметрическими полиномами (степень ≤8, коэффициенты по возрастанию), многоугольники с малым числом вершин — точные отрезки, текст → `formulas`. `examples/12_polyart_lang.plang` дополнен, экспортировано 42 объекта. Рендер из `.polyart` через `polyart_format.render_polyart` работает; CV: оригинал 63.4, рендер 59.8 — оба ART |

---

## 2026-08-02 — CV-проверка (Python + Chrome)

| ID | Статус | Проверка | Результат |
|----|--------|----------|-----------|
| C01 | ПРОВЕРЕНО | CV-анализ 13 сгенерированных изображений (ImageFeatureExtractor + RarityScorer из `polyart_cv_test.py`) | Все 13 → ART, средняя редкость 61.4/100. В т.ч. кисть: oil_brush_twin 65.6, sunset 62.9, forest 68.3, winter 58.7; мета-язык 12_polyart_lang 63.4 |
| C02 | ПРОВЕРЕНО | HTML-галерея `cv_output_gallery.html` открыта в Chromium (Playwright) | Галерея с изображениями, классом, редкостью и 8 фичами открылась в Chrome |
| C03 | ПРОВЕРЕНО | JSON-отчёт `cv_output_report.json` | Сохранён: 13 записей (фича, class, rarity) |
| C04 | ИСПРАВЛЕНО | Старые CV-скрипты ссылаются на несуществующий путь | `cv_verify_all.py` и `cv_check_emperor.py` содержат `C:\Users\e\Desktop\6756756756756756` — папка отсутствует. Пути переведены на BASE_DIR + PNG_DIR-аргумент; оба скрипта работают |
| C05 | ИСПРАВЛЕНО | 3D-демо сохраняли JSON под именем `.png` | `polyart_3d.py` через `canvas.save("demo_*.png")` писал polyart JSON в файлы `.png` (3 шт., ~850 КБ), причём с сырыми массивами точек в `PolyObj` — неконформно формату и против принципа «экономии связи». Исправлено: `Scene3D.render_to_canvas` заменён на `Scene3D.render(path)` — прямое прорисовывание каркасов/поверхностей в PNG; JSON-дампы удалены, демо-функции остаются компактным «упоминанием» примитивов с координатами. README и REGISTRY обновлены |
| C06 | ПРОВЕРЕНО | Полный CV-скан репозитория (`cv_verify_all.py`, 41 PNG) | TOTAL: 41 \| ART: 36 \| LIVING: 1 \| BORDERLINE: 4 \| ERR: 0. Ранее ERR: 3 — это были мисназванные JSON из C05; теперь это настоящие PNG (проверены магические байты). Средняя редкость 55.8/100 |
| C07 | НАЙДЕНО | Две разные схемы `.polyart` в проекте | (а) каноническая v2.0.0 (polyart_api.save / polyart_format): `meta/canvas/layers/formulas`, объекты с `type`, коэффициенты по возрастанию; (б) схема конвертера (`polyart_converter.convert`, v1.0): `width/height/objects` с `kind:"polyline"`, коэффициенты в порядке `np.polyfit` (по убыванию). Схемы не взаимозаменяемы. Мост `save_polyart` пишет каноническую (а); конвертер читает свою (б). Сведение схем — отдельная задача конвертера (рекомендуется единый v2.0.0) |

---

## 2026-08-02 — Мост «растр → мета-текст» (MetaDesk) и лазерный ландшафт

| ID | Статус | Проверка | Результат |
|----|--------|----------|-----------|
| M01 | ПРОВЕРЕНО | `polyart_metadesc.py`: изображение → мета-текстовое описание `.plang` | Постеризация (k-средние в LAB) + cv2.findContours → замкнутые области; каждая → `polygon({координаты})` с комментарием-классификацией (circle/polygon/curve) и цветом. oil_brush_twin_demo.png → 17 областей, текст 21 КБ против PNG 157 КБ (экономия связи ~7.5×). Рендер → PNG + save_polyart → `.polyart`. CV: oil_meta 55.2 ART, geom_meta 53.9 ART |
| M02 | ИСПРАВЛЕНО | Баг Ramer–Douglas–Peucker: полилинии схлопывались в 1 точку | Базовый случай `recurse(i,j)` возвращал `[pts[i]]` вместо `[pts[i], pts[j]]`, из-за чего на стыках рекурсии терялись вершины (квадрат 4 точки → 1). Исправлено: квадрат → 4, кривые сохраняют форму |
| M03 | ПРОВЕРЕНО | `polyart_relief.py`: лазерное сканирование → ландшафт `.plang` | Рельеф = энергия мазков (импасто); горы = пики по выпуклости (prominence) с отсечением рамки кадра; озёра = гладкие тёмные зоны относительно фона. Горы распределены по нижней трети (там текстура мазков), а не сгружены у верхнего гребня. CV: oil_terrain 60.3 ART, топокарта 61.2 ART |

---

## Замечания по методу

- `ЛОГИЧЕСКАЯ СХОДИМОСТЬ` = документация, грамматика, парсер, интерпретатор,
  конвертер и UML должны описывать одно и то же поведение.
- Статусы: НАЙДЕНО → ИСПРАВЛЕНО → ПРОВЕРЕНО.
